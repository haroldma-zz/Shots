<Page
    x:Class="Shots.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Shots.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utilities="using:Shots.Utilities"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding Source={StaticResource Locator}, Path=Main}">

    <Grid>
        <utilities:ScrollListView x:Name="FeedView" ItemsSource="{Binding Feed}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.Resources>
                            <utilities:ParallaxConverter x:Key="ParallaxConverter" 
                                                             Item="{Binding}" 
                                                             List="{Binding ElementName=FeedView}"/>
                        </Grid.Resources>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid Background="Black">
                            <Grid>
                                <Grid.Background>
                                    <ImageBrush Stretch="UniformToFill"
                                            utilities:BlurImageTool.Source="{Binding Resource.Pic1080}">
                                        <ImageBrush.Transform>
                                            <TranslateTransform Y="{Binding ElementName=FeedView, Path=ScrollViewer.VerticalOffset, Converter={StaticResource ParallaxConverter}}"/>
                                        </ImageBrush.Transform>
                                    </ImageBrush>
                                </Grid.Background>
                                <StackPanel Orientation="Horizontal" Margin="10">
                                    <Ellipse Width="50" Height="50">
                                        <Ellipse.Fill>

                                            <ImageBrush ImageSource="{Binding User.ProfilePhotoSmall}" />
                                        </Ellipse.Fill>
                                    </Ellipse>
                                    <TextBlock Text="{Binding User.Username}" FontSize="25" Foreground="White"
                                           VerticalAlignment="Center" Margin="10,0,0,0" />
                                </StackPanel>
                            </Grid>
                        </Grid>
                        <Image Grid.Row="1" Height="{Binding Resource.RatioHeight}" Source="{Binding Resource.Pic1080}" />
                        <Grid Background="Black" Grid.Row="2">
                            <Grid>
                                <Grid.Background>
                                    <ImageBrush Stretch="UniformToFill"
                                            utilities:BlurImageTool.Source="{Binding Resource.Pic1080}">
                                        <ImageBrush.Transform>
                                            <TranslateTransform Y="{Binding ElementName=FeedView, Path=ScrollViewer.VerticalOffset, Converter={StaticResource ParallaxConverter}}"/>
                                        </ImageBrush.Transform>
                                    </ImageBrush>
                                </Grid.Background>
                                <Grid Margin="10">
                                    <TextBlock Text="{Binding Resource.Description}" FontSize="25" Foreground="White"
                                           VerticalAlignment="Center" TextWrapping="Wrap"/>
                                </Grid>
                            </Grid>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </utilities:ScrollListView>
    </Grid>
</Page>